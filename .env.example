# Copy this file to .env and replace the values for your local environment.
# These are portfolio-safe placeholders; do not commit real credentials.

API_URL=https://example.com/api/
API_USER=collector
API_PASSWORD=changeme
